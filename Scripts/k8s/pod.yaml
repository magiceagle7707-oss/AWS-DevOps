# Simple example yml for a Pod with two containers, nginx and jenkins

apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80
  - name: jenkins
    image: jenkins/jenkins
    ports:
    - containerPort: 8080